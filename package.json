{
  "name": "modlogger",
  "version": "1.1.0",
  "description": "Discord moderation tracker bot (Render-compatible, no DB).",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "discord.js": "^14.16.0",
    "node-cron": "^3.0.3"
  },
  "engines": {
    "node": ">=18.x"
  }
}

